---
import Title from "../components/Title.astro";
import Paragraph from "../components/Text.astro";
import { t } from "../i18n";

const lang =
  Astro.request.headers.get("cookie")?.match(/preferredLang=([^;]+)/)?.[1] ||
  "es";
---

<div class="relative lg:flex items-center">
  <div class="max-w-[800px] text-white px-0">
    <div
      class="lg:bg-[#0B0B0B]/60 ps-6 pe-6 py-6 px-4 lg:ps-32 lg:pe-24 lg:py-10"
    >
      <Title text={t(lang, "pages.about.title")} />
      <Paragraph
        class="text-justify"
        text={t(lang, "pages.about.description")}
      />
    </div>
  </div>

  <div class="w-full flex justify-center mt-32 lg:mt-0 xs:relative">
    <div
      class="neon-box border rounded-xs border-[#daa032] h-[250px] w-[190px] absolute z-2 -translate-x-12 -translate-y-8 lg:h-[400px] lg:w-[300px] lg:-translate-x-[100px] lg:-translate-y-[50px]"
    >
    </div>

    <div
      class="bg-[#0B0B0B]/60 h-[250px] w-[190px] absolute z-2 translate-x-12 lg:h-[400px] lg:w-[300px] lg:translate-x-[100px]"
    >
    </div>

    <img
      src="/images/aboutme.png"
      alt="Eloy Lozano retrato"
      class="shadow-lg max-h-62 object-cover z-10 translate-y-12 lg:max-h-100  lg:translate-y-12"
      loading="lazy"
    />
  </div>
</div>

<style>
  @keyframes neonPulse {
    0%,
    100% {
      box-shadow:
        0 0 6px #daa032,
        0 0 30px #daa032,
        inset 0 0 12px #daa032;
    }
    50% {
      box-shadow:
        0 0 6px #daa032,
        0 0 15px #daa032,
        inset 0 0 12px #daa032;
    }
  }

  .neon-box {
    animation: neonPulse 3s ease-in-out infinite;
    box-shadow:
      0 0 6px #daa032,
      0 0 30px #daa032,
      inset 0 0 8px #daa032;
  }
</style>
